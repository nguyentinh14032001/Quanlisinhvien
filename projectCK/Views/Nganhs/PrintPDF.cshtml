@model List<projectCK.Models.Nganh>
<style>
    thead {
        background-color: #00FF00;
        color: black
    }

    table {
        width: 100%;
        border: 1px solid black;
        font-size: 50px;
        margin-left: auto;
        margin-right: auto;
    }

    td, th {
        padding: 5px;
        font-size: 14px;
    }
</style>
<table class="table table-bordered table-striped" border="1" style="border-color:black">
    <thead>
        <tr>
            <th>Mã ngành</th>
            <th>Tên ngành</th>
            <th>Tên khoa</th>
            <th>Tổng số tín chỉ</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MaNganh</td>
                <td>@item.TenNganh</td>
                <td>@item.Khoa.TenKhoa</td>
                <td>@item.TongSTC</td>
            </tr>

        }
    </tbody>
</table>